const triangulo = (length, width) =>{
    let area = [];
       area[0]= length*width
       area[1]= 2*(length+width)
    return area;
}



function getArea(length, width) {
    let area = [];
       area[0]= length*width
       area[1]= 2*(length+width)
    return area;
  
}